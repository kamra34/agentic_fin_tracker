# Database Configuration (PostgreSQL)
# For local development:
DATABASE_URL=postgresql://username:password@localhost:5432/financial_tracker
# For Railway (provided automatically when you link PostgreSQL service):
# DATABASE_URL=postgresql://postgres:xxx@xxx.railway.app:5432/railway

# Security - IMPORTANT: Generate a new secret key for production!
# Run: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=your-secret-key-here-change-this-in-production
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# CORS - Add your production frontend URL
# For local development:
ALLOWED_ORIGINS=http://localhost:5174,http://localhost:3000
# For production (update with your Vercel URL):
# ALLOWED_ORIGINS=https://your-app.vercel.app,http://localhost:5174
